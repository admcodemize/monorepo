import { FAMILIY, SIZES } from "@codemize/constants/Fonts";
import { StyleSheet } from "react-native"

/**
 * @public
 * @author Marc Stöckli - Codemize GmbH 
 * @since 0.0.13
 * @version 0.0.1 */
const TouchableCheckboxStyle = StyleSheet.create({
  container: {
    marginLeft: 10,
  },
  innerIcon: {
    borderWidth: 1
  }
})

export default TouchableCheckboxStyle;