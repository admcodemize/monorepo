import { StyleSheet } from "react-native"

/**
 * @public
 * @author Marc Stöckli - Codemize GmbH 
 * @since 0.0.2
 * @version 0.0.1 */
const CalendarBlockedScopeStyle = StyleSheet.create({
  view: {
    position: "absolute",
    opacity: 0.5,
    zIndex: 10
  }
})

export default CalendarBlockedScopeStyle;