import { StyleSheet } from "react-native"

/**
 * @public
 * @author Marc Stöckli - Codemize GmbH 
 * @since 0.0.14
 * @version 0.0.4 */
const TouchableHapticGoogleStyle = StyleSheet.create({
  view: {
    padding: 6, 
    borderRadius: 4,
    gap: 4
  }
})

export default TouchableHapticGoogleStyle;