import { StyleSheet } from "react-native"

/**
 * @public
 * @author Marc Stöckli - Codemize GmbH 
 * @since 0.0.18
 * @version 0.0.1 */
const TouchableTagStyle = StyleSheet.create({
  view: {
    gap: 4,
    padding: 6, 
    paddingVertical: 4, 
    borderRadius: 4
  }
})

export default TouchableTagStyle;