import { StyleSheet } from "react-native"

/**
 * @public
 * @author Marc Stöckli - Codemize GmbH 
 * @since 0.0.8
 * @version 0.0.1 */
const ImagePlaceholderStyle = StyleSheet.create({
  container: {
    gap: 20
  }
});

export default ImagePlaceholderStyle;