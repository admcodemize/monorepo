import { StyleSheet } from "react-native";

/**
 * @public
 * @author Marc Stöckli - Codemize GmbH 
 * @since 0.0.1
 * @version 0.0.1 */
const TouchableHapticCloseStyle = StyleSheet.create({
  close: {
    borderRadius: 20,
    borderWidth: 1,
    padding: 4
  }
})

export default TouchableHapticCloseStyle;